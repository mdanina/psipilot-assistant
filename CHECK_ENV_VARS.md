# Проверка переменных окружения на сервере

## Проблема

Переменная есть в `.env`, но все равно не работает? Это значит, что она **не была встроена во время сборки**.

**Vite встраивает переменные ВО ВРЕМЯ `npm run build`, а не во время выполнения!**

## Диагностика на сервере

### Шаг 1: Проверьте что переменная действительно в .env

```bash
cd /opt/psipilot
grep VITE_N8N_SUPERVISOR_WEBHOOK_URL .env
```

Должно показать:
```
VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://cigadusiref.beget.app/webhook/0c857cb1-9655-4af0-96ec-b1e1bc93b280
```

**Проверьте что НЕТ пробелов после `=`:**
```bash
# Если видите пробел после = - это проблема!
# Неправильно: VITE_N8N_SUPERVISOR_WEBHOOK_URL= https://...
# Правильно: VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://...
```

### Шаг 2: Проверьте что переменная ВСТРОЕНА в сборку

```bash
cd /opt/psipilot

# Проверьте наличие переменной в собранном коде
grep -r "VITE_N8N_SUPERVISOR_WEBHOOK_URL" dist/assets/*.js | head -1

# ИЛИ проверьте что ваш URL есть в коде
grep -r "cigadusiref.beget.app" dist/assets/*.js | head -1
```

**Если переменная НЕ найдена в `dist/assets/*.js`:**
- ❌ Она НЕ была встроена во время сборки
- ⚠️ Нужно ПЕРЕСОБРАТЬ с установленной переменной

**Если переменная найдена, но значение неправильное:**
- ⚠️ Нужно ПЕРЕСОБРАТЬ с правильным значением

### Шаг 3: Проверьте когда была последняя сборка

```bash
ls -la dist/assets/*.js | head -1
```

Посмотрите дату изменения файлов. Если она **раньше**, чем вы добавили переменную в `.env` - нужно пересобрать.

### Шаг 4: Проверьте что переменная доступна во время сборки

```bash
cd /opt/psipilot

# Загрузите переменные из .env
export $(grep -v '^#' .env | grep '^VITE_' | sed 's/[[:space:]]*=[[:space:]]*/=/' | xargs)

# Проверьте что она установлена
echo "VITE_N8N_SUPERVISOR_WEBHOOK_URL: $VITE_N8N_SUPERVISOR_WEBHOOK_URL"

# ИЛИ используйте Node для проверки
node -e "require('dotenv').config(); console.log('URL:', process.env.VITE_N8N_SUPERVISOR_WEBHOOK_URL)"
```

**Если переменная пустая (`undefined` или пустая строка):**
- ⚠️ Проблема в формате `.env` файла
- Проверьте пробелы, кавычки, правильность имени переменной

## Решение: Пересборка с правильными переменными

**ВАЖНО:** Даже если переменная есть в `.env`, если сборка была сделана ДО её добавления, нужно пересобрать!

```bash
cd /opt/psipilot

# 1. Загрузите переменные из .env (исправляя пробелы)
export $(grep -v '^#' .env | grep '^VITE_' | sed 's/[[:space:]]*=[[:space:]]*/=/' | xargs)

# 2. Исправьте VITE_TRANSCRIPTION_API_URL если есть пробел
VITE_TRANSCRIPTION_API_URL=$(echo "$VITE_TRANSCRIPTION_API_URL" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//')
export VITE_TRANSCRIPTION_API_URL

# 3. ПРОВЕРЬТЕ что переменные установлены
echo "=== Проверка переменных ==="
echo "VITE_SUPABASE_URL: ${VITE_SUPABASE_URL:0:40}..."
echo "VITE_SUPABASE_ANON_KEY: ${VITE_SUPABASE_ANON_KEY:0:30}..."
echo "VITE_N8N_SUPERVISOR_WEBHOOK_URL: ${VITE_N8N_SUPERVISOR_WEBHOOK_URL:0:50}..."

# Если какая-то переменная пустая - ОСТАНОВИТЕСЬ и исправьте .env файл!

# 4. Очистите старую сборку
rm -rf dist node_modules/.vite

# 5. ПЕРЕСОБЕРИТЕ (переменные уже экспортированы)
npm run build

# 6. ПРОВЕРЬТЕ что переменные встроились
echo "=== Проверка встроенных переменных ==="
grep -r "cigadusiref.beget.app" dist/assets/*.js && echo "✅ Supervisor webhook URL найден в сборке!" || echo "❌ URL НЕ найден - переменная не встроилась!"
```

## После пересборки

1. **В браузере:**
   - Откройте консоль (F12)
   - Выполните Hard Refresh: `Ctrl + Shift + R` (Windows/Linux) или `Cmd + Shift + R` (Mac)
   - Проверьте диагностику переменных окружения в консоли

2. **Проверка в браузере (консоль):**
   - Должна появиться диагностика: "🔍 Диагностика переменных окружения"
   - `VITE_N8N_SUPERVISOR_WEBHOOK_URL` должен быть помечен как "установлен"

## Частые ошибки

1. **Переменная добавлена в .env ПОСЛЕ сборки** → Нужно пересобрать
2. **Пробел после `=`** → `VITE_N8N_SUPERVISOR_WEBHOOK_URL= https://...` (неправильно)
3. **Переменная не экспортирована перед `npm run build`** → Используйте `export`
4. **Старая сборка в dist/** → Удалите `dist` перед пересборкой
5. **Кэш браузера** → Выполните Hard Refresh
