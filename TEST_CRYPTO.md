# Тестирование исправлений crypto API

## Быстрый старт

### 1. Убедитесь, что backend сервер запущен

```bash
cd backend/transcription-service
npm run dev
```

Сервер должен быть доступен на `http://localhost:3001`

### 2. Запустите тесты

```bash
cd backend/transcription-service
npm run test:crypto
```

Или напрямую:

```bash
node backend/transcription-service/test-crypto.js
```

## Что тестируется

### ✅ Тест 1: Прямое шифрование/расшифровка
- Проверяет работу функций `encrypt()` и `decrypt()` напрямую
- Валидирует, что зашифрованные данные корректно расшифровываются

### ✅ Тест 2: Валидация формата данных
- Проверяет обработку пустых строк
- Проверяет обработку невалидного base64
- Проверяет обработку слишком коротких строк

### ✅ Тест 3: API шифрование/расшифровка
- Тестирует `/api/crypto/encrypt` endpoint
- Тестирует `/api/crypto/decrypt` endpoint
- Проверяет корректность работы через HTTP API

### ✅ Тест 4: Batch операции
- Тестирует batch шифрование массива данных
- Тестирует batch расшифровку массива данных
- Проверяет, что все элементы корректно обрабатываются

### ✅ Тест 5: Обработка ошибок
- Тестирует обработку невалидных данных в batch
- Проверяет, что невалидные элементы возвращают пустые строки
- Проверяет наличие warnings в ответе

### ✅ Тест 6: Смешанные данные
- Тестирует batch с валидными и невалидными данными одновременно
- Проверяет, что валидные данные расшифровываются, а невалидные обрабатываются корректно

## Ожидаемый результат

Все тесты должны пройти успешно:

```
🚀 Запуск тестов crypto API
==================================================
✅ Сервер доступен на http://localhost:3001

🧪 Тест: Прямое шифрование/расшифровка (без API)
✅ Прямое шифрование/расшифровка работает корректно

🧪 Тест: Валидация формата данных
✅ Валидация работает корректно

🧪 Тест: API: Шифрование и расшифровка одиночного значения
✅ API шифрование/расшифровка работает корректно

🧪 Тест: API: Batch шифрование и расшифровка
✅ Batch шифрование/расшифровка работает корректно

🧪 Тест: Обработка ошибок: невалидные данные
✅ Невалидные данные корректно обработаны (возвращены пустые строки)
✅ Warnings корректно возвращаются

🧪 Тест: Обработка смешанных данных (валидные + невалидные)
✅ Валидные данные успешно расшифрованы
✅ Невалидные данные корректно обработаны
✅ Смешанные данные обработаны корректно

==================================================
🎉 Все тесты пройдены! (6/6)
```

## Тестирование через браузер (фронтенд)

### 1. Откройте консоль разработчика (F12)

### 2. Выполните тесты в консоли:

```javascript
// Импортируем функции (если доступны)
import { encryptPHI, decryptPHI, decryptPHIBatch } from '@/lib/encryption';

// Тест 1: Одиночное шифрование/расшифровка
const test1 = async () => {
  const plaintext = 'Тестовые данные';
  const encrypted = await encryptPHI(plaintext);
  console.log('Зашифровано:', encrypted);
  const decrypted = await decryptPHI(encrypted);
  console.log('Расшифровано:', decrypted);
  console.log('Совпадает:', decrypted === plaintext);
};
await test1();

// Тест 2: Batch расшифровка
const test2 = async () => {
  const plaintexts = ['Первая строка', 'Вторая строка', 'Третья строка'];
  const encrypted = await Promise.all(plaintexts.map(p => encryptPHI(p)));
  console.log('Зашифровано:', encrypted);
  const decrypted = await decryptPHIBatch(encrypted);
  console.log('Расшифровано:', decrypted);
  console.log('Совпадает:', decrypted.every((d, i) => d === plaintexts[i]));
};
await test2();

// Тест 3: Обработка ошибок (старые данные)
const test3 = async () => {
  const oldData = ['старые-данные-зашифрованные-старым-ключом'];
  try {
    const decrypted = await decryptPHIBatch(oldData);
    console.log('Результат:', decrypted);
    console.log('Все пустые:', decrypted.every(d => d === ''));
  } catch (error) {
    console.error('Ошибка:', error);
  }
};
await test3();
```

## Тестирование в реальном приложении

### 1. Проверьте работу с новыми данными

1. Создайте новую сессию
2. Добавьте транскрипцию
3. Создайте клиническую заметку
4. Проверьте, что данные корректно отображаются

### 2. Проверьте обработку старых данных

1. Откройте страницу с существующими сессиями (старыми тестовыми данными)
2. Проверьте, что страница не падает с ошибкой
3. Проверьте логи в консоли браузера - должны быть предупреждения, но не ошибки

### 3. Проверьте Network tab

1. Откройте DevTools → Network
2. Фильтруйте по "decrypt"
3. Проверьте, что запросы возвращают статус 200 (не 500)
4. Проверьте ответы - должны быть успешными даже при наличии warnings

## Устранение проблем

### Ошибка: "ENCRYPTION_KEY environment variable is not set"

**Решение:** Убедитесь, что в `.env` файле бэкенда установлен `ENCRYPTION_KEY`:

```bash
cd backend/transcription-service
npm run generate-key
# Скопируйте сгенерированный ключ в .env файл
```

### Ошибка: "Сервер не доступен"

**Решение:** 
1. Убедитесь, что backend сервер запущен
2. Проверьте, что порт 3001 свободен
3. Проверьте URL в тесте (по умолчанию `http://localhost:3001`)

### Ошибка: "Not authenticated" (при тестировании через API)

**Решение:** 
1. Для базовых тестов аутентификация не требуется (тесты работают напрямую с функциями)
2. Для полного тестирования API можно установить `TEST_TOKEN` в переменных окружения

### Тесты падают на обработке ошибок

**Решение:** 
- Проверьте, что исправления применены корректно
- Убедитесь, что сервер перезапущен после изменений
- Проверьте логи сервера для деталей ошибок

## Дополнительные проверки

### Проверка логов сервера

При запуске тестов следите за логами backend сервера. Должны быть предупреждения вида:

```
[Batch decrypt] Failed to decrypt item 1: Failed to decrypt data: data may be corrupted...
[Batch decrypt] 2 out of 4 items failed to decrypt
```

Это нормально для тестов с невалидными данными.

### Проверка в базе данных

Можно проверить, что новые данные корректно сохраняются:

```sql
-- Проверить наличие зашифрованных данных
SELECT 
  id,
  LENGTH(ai_content_encrypted) as encrypted_length,
  ai_content IS NULL as is_decrypted
FROM sections 
WHERE ai_content_encrypted IS NOT NULL 
LIMIT 5;
```

## Результат

После успешного прохождения всех тестов:

✅ Новые данные корректно шифруются и расшифровываются  
✅ Старые данные не вызывают ошибок (возвращаются пустые строки)  
✅ Batch операции не падают на одном проблемном элементе  
✅ Система устойчива к невалидным данным  

Система готова к использованию! 🎉





