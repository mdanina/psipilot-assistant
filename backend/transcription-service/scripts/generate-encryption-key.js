#!/usr/bin/env node

/**
 * Скрипт для генерации ключа шифрования
 * Использование: node scripts/generate-encryption-key.js
 */

import crypto from 'crypto';

// Генерируем 32-байтовый ключ (256 бит)
const key = crypto.randomBytes(32).toString('base64');

console.log('\n🔐 Сгенерированный ключ шифрования:\n');
console.log(key);
console.log('\n📝 Добавьте этот ключ в ваш .env файл:');
console.log(`ENCRYPTION_KEY=${key}\n`);
console.log('⚠️  ВАЖНО: Храните этот ключ в безопасности! Не коммитьте в git!\n');
