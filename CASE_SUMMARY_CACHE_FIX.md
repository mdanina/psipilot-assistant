# ✅ Исправление кеширования case_summary и оптимизация usePatient

## 🎉 Что сделано:

1. ✅ **Создан React Query hook `usePatientCaseSummary()`** для загрузки сводки по случаю
   - Автоматическое кеширование на 5 минут
   - Расшифровка данных кешируется вместе с данными
   - Дедупликация запросов

2. ✅ **Мигрирован `CaseSummaryBlock` на React Query**
   - Использует `usePatientCaseSummary()` вместо прямого запроса
   - Данные кешируются автоматически
   - При повторной загрузке страницы запросов не будет

3. ✅ **Улучшено кеширование `usePatient()`**
   - Увеличен `staleTime` до 10 минут
   - Увеличен `gcTime` до 30 минут
   - Данные дольше остаются в кеше

4. ✅ **Исправлена конфигурация `query-client.ts`**
   - Используется `gcTime` вместо устаревшего `cacheTime`

## 🧪 Как проверить:

### Тест 1: Проверка кеширования при повторной загрузке

1. Открыть DevTools (`F12`) → вкладка **Network**
2. Применить фильтр **"Fetch/XHR"**
3. Очистить запросы (кнопка 🚫)
4. Открыть страницу пациента (например, `/patients/9da1c...`)
5. Проверить запросы:
   - `patients?select=*&id=eq.9da1c...` - должен быть **1 раз** ✅
   - `patients?select=case_summary_...` - должен быть **1 раз** ✅
   - `decrypt` - должен быть **1 раз** ✅
6. Перейти на другую страницу (например, `/patients`)
7. Вернуться на страницу пациента
8. Проверить запросы снова

**Ожидаемый результат:**
- ✅ При повторной загрузке запросов к `patients?select=*&id=eq...` **не должно быть** (кеш работает)
- ✅ При повторной загрузке запросов к `patients?select=case_summary_...` **не должно быть** (кеш работает)
- ⚠️ `decrypt` может все еще загружаться, если данные не в кеше (но теперь расшифрованные данные кешируются)

### Тест 2: Проверка дедупликации

1. Открыть страницу пациента
2. Быстро перезагрузить страницу несколько раз (`Ctrl+R`)
3. Проверить Network tab

**Ожидаемый результат:**
- ✅ Запрос к `patients?select=*&id=eq...` должен быть **1 раз** (дедупликация работает)
- ✅ Запрос к `patients?select=case_summary_...` должен быть **1 раз** (дедупликация работает)

---

## 📊 Ожидаемые улучшения:

### До исправления:
- ❌ `patients?select=*&id=eq...` - каждый раз при загрузке страницы
- ❌ `patients?select=case_summary_...` - каждый раз при загрузке страницы (дублировался 2 раза)
- ❌ `decrypt` - каждый раз при загрузке страницы (дублировался 2 раза)

### После исправления:
- ✅ `patients?select=*&id=eq...` - **1 раз** при первой загрузке, **0 раз** при повторной (кеш работает)
- ✅ `patients?select=case_summary_...` - **1 раз** при первой загрузке, **0 раз** при повторной (кеш работает)
- ✅ `decrypt` - **1 раз** при первой загрузке, **0 раз** при повторной (расшифрованные данные кешируются)

---

## ⚠️ Важные замечания:

### Расшифровка данных (`decrypt`)
Теперь расшифровка происходит внутри React Query hook и результат кешируется. Это означает:
- При первой загрузке: запрос к БД + расшифровка
- При повторной загрузке: данные из кеша (без запросов)

### Время кеширования
- `staleTime: 10 минут` - данные считаются свежими 10 минут
- `gcTime: 30 минут` - данные хранятся в кеше 30 минут после последнего использования

---

## ✅ Готово!

Теперь и основная информация о пациенте, и сводка по случаю кешируются через React Query!

**Проверьте повторную загрузку страницы - запросов к `patients` и `case_summary` быть не должно!**

