# 🧪 Простой тест кеширования (5 минут)

## Быстрая проверка, что кеширование работает

### Шаг 1: Открыть приложение
```bash
npm run dev
```

### Шаг 2: Открыть DevTools
- Нажать `F12`
- Перейти на вкладку **Network** (Сеть)

### Шаг 3: Тест кеширования

1. **Открыть страницу пациентов:**
   - Перейти на `/patients`
   - В Network tab должен быть запрос к Supabase (обычно `rest/v1/patients`)
   - ✅ Записать: "Первый раз - запрос есть"

2. **Перейти на другую страницу:**
   - Перейти на `/sessions` или любую другую страницу

3. **Вернуться на страницу пациентов:**
   - Вернуться на `/patients`
   - В Network tab НЕ должно быть нового запроса к `patients`
   - ✅ Записать: "Второй раз - запроса нет (кеш работает!)"

### Результат

✅ **Если запроса нет** → Кеширование работает!  
⚠️ **Если запрос есть** → Проверьте:
- Прошло ли больше 5 минут между загрузками?
- Правильно ли настроен `query-client.ts`?
- Используются ли компоненты React Query hooks?

---

## Дополнительная проверка (опционально)

### Проверить в Console

Открыть Console (`F12` → Console) и выполнить:

```javascript
// Проверить, что React Query установлен
console.log('React Query:', typeof window !== 'undefined' && window.__REACT_QUERY_CLIENT__);
```

Если видите `undefined` - это нормально (React Query работает через провайдер).

---

## Что делать, если кеш не работает?

1. **Проверить файл `src/lib/query-client.ts`:**
   - Убедиться, что файл существует
   - Проверить, что `staleTime` установлен (5 минут)

2. **Проверить файл `src/App.tsx`:**
   - Убедиться, что используется `queryClient` из `@/lib/query-client`
   - Проверить, что `QueryClientProvider` обернут вокруг приложения

3. **Проверить Console на ошибки:**
   - Открыть Console (`F12` → Console)
   - Посмотреть, нет ли ошибок

4. **Откатить изменения (если нужно):**
   ```bash
   git checkout HEAD -- src/App.tsx src/lib/query-client.ts
   ```

---

**Готово!** Если кеш работает - изменения применены успешно! 🎉

