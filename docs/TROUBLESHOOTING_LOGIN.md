# Решение проблем с входом в систему

## Проблема: Не могу войти, хотя пользователь создан и профиль добавлен

### Возможные причины:

1. **RLS политики блокируют доступ к профилю**
2. **Проблема с получением профиля через JOIN с clinics**
3. **Отсутствует clinic_id у пользователя**
4. **Ошибка в запросе fetchProfile**

## Диагностика

### Шаг 1: Проверьте консоль браузера

Откройте DevTools (F12) → Console и посмотрите на логи:

**Ожидаемые логи при успешном входе:**
```
✅ Sign in successful, fetching profile...
🔍 Fetching profile for user: <user-id>
✅ Profile fetched successfully: { id, email, full_name, role, clinic_id, has_clinic }
✅ User authenticated successfully
🔄 Auth state change: SIGNED_IN
```

**Если видите ошибки:**
- `❌ Error fetching profile:` - проблема с получением профиля
- `❌ Sign in error:` - проблема с аутентификацией
- Проверьте детали ошибки (code, message, details, hint)

### Шаг 2: Проверьте пользователя в базе данных

Выполните в Supabase Dashboard → SQL Editor:

```sql
-- Замените 'user-email@example.com' на email пользователя
SELECT 
    au.id as auth_user_id,
    au.email,
    au.email_confirmed_at,
    p.id as profile_id,
    p.email as profile_email,
    p.full_name,
    p.role,
    p.clinic_id,
    CASE 
        WHEN p.id IS NULL THEN '❌ Profile missing'
        WHEN p.clinic_id IS NULL THEN '⚠️  No clinic_id'
        ELSE '✅ OK'
    END as status
FROM auth.users au
LEFT JOIN profiles p ON au.id = p.id
WHERE au.email = 'user-email@example.com';
```

### Шаг 3: Проверьте RLS политики

```sql
-- Проверяем политики для profiles
SELECT 
    policyname,
    cmd,
    qual
FROM pg_policies
WHERE tablename = 'profiles';
```

Должны быть политики:
- `Users can view own profile` - для SELECT с условием `id = auth.uid()`
- `Users can view clinic profiles` - для SELECT с условием `clinic_id = get_user_clinic_id()`

### Шаг 4: Проверьте, может ли пользователь получить свой профиль

```sql
-- ВАЖНО: Замените 'USER_ID_HERE' на UUID пользователя из шага 2
-- Выполните этот запрос от имени пользователя (через API с его токеном)
-- Или временно отключите RLS для теста:

-- ВРЕМЕННО для диагностики (не используйте в production!)
ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;

-- Попробуйте войти снова

-- Включите обратно после диагностики
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
```

## Решения

### Решение 0: Профиль создан без ID или email

**Симптомы:**
- В профиле `id = NULL` или `email = NULL`
- `fetchProfile` не может найти профиль
- Вход не работает

**Решение:**
Выполните скрипт `scripts/fix-user-profile.sql` в Supabase Dashboard → SQL Editor:

1. Замените `'mdanina@yandex.ru'` на email пользователя
2. Выполните все запросы по порядку
3. Проверьте результат в шаге 5

Или вручную:

```sql
-- Найти пользователя
SELECT id, email FROM auth.users WHERE email = 'user@example.com';

-- Обновить профиль (замените USER_ID на UUID из запроса выше)
UPDATE profiles
SET 
    id = 'USER_ID',
    email = 'user@example.com'
WHERE email IS NULL OR id IS NULL;
```

### Решение 1: У пользователя нет clinic_id

Если у пользователя `clinic_id IS NULL`, это может вызвать проблемы с JOIN к clinics.

**Вариант A: Назначить пользователю клинику**

```sql
-- Сначала создайте клинику, если её нет
INSERT INTO clinics (name, email)
VALUES ('Default Clinic', 'clinic@example.com')
ON CONFLICT DO NOTHING
RETURNING id;

-- Затем назначьте пользователю клинику
-- Замените 'user-email@example.com' и 'CLINIC_ID' на реальные значения
UPDATE profiles
SET clinic_id = 'CLINIC_ID'
WHERE email = 'user-email@example.com'
  AND clinic_id IS NULL;
```

**Вариант B: Изменить запрос fetchProfile, чтобы он работал без clinic_id**

Запрос уже использует LEFT JOIN, так что это должно работать. Но если проблема в RLS политике для clinics, можно попробовать:

```sql
-- Проверьте RLS политики для clinics
SELECT policyname, cmd, qual
FROM pg_policies
WHERE tablename = 'clinics';
```

### Решение 2: Проблема с RLS политикой

Если RLS блокирует доступ, проверьте:

1. **Пользователь может видеть свой профиль:**
   ```sql
   -- Политика должна быть:
   CREATE POLICY "Users can view own profile"
       ON profiles FOR SELECT
       USING (id = auth.uid());
   ```

2. **Если у пользователя нет clinic_id, политика "Users can view clinic profiles" не сработает:**
   - Это нормально, так как есть политика "Users can view own profile"
   - Но если она не работает, проверьте, что `auth.uid()` возвращает правильный UUID

### Решение 3: Временное отключение RLS для диагностики

**⚠️ ВНИМАНИЕ: Только для диагностики! Не используйте в production!**

```sql
-- Временно отключите RLS
ALTER TABLE profiles DISABLE ROW LEVEL SECURITY;

-- Попробуйте войти

-- Если вход работает, проблема в RLS политиках
-- Включите обратно и исправьте политики
ALTER TABLE profiles ENABLE ROW LEVEL SECURITY;
```

### Решение 4: Проверка функции get_user_clinic_id

Если пользователь не может получить профиль из-за JOIN с clinics, проверьте функцию:

```sql
-- Проверьте функцию
SELECT 
    routine_name,
    routine_definition
FROM information_schema.routines
WHERE routine_name = 'get_user_clinic_id';

-- Протестируйте функцию (замените USER_ID)
SELECT get_user_clinic_id();
```

## Детальная диагностика

Используйте скрипт `scripts/diagnose-login-issue.sql` для полной диагностики:

1. Откройте Supabase Dashboard → SQL Editor
2. Замените `'user-email@example.com'` на email пользователя
3. Замените `'USER_ID_HERE'` на UUID пользователя
4. Выполните все запросы по порядку
5. Проанализируйте результаты

## Частые проблемы

### Проблема: "Error fetching profile: new row violates row-level security policy"

**Причина:** RLS политика блокирует доступ к профилю.

**Решение:**
1. Проверьте, что политика "Users can view own profile" существует
2. Проверьте, что `auth.uid()` возвращает правильный UUID
3. Убедитесь, что пользователь действительно аутентифицирован

### Проблема: "Profile query returned no data"

**Причина:** Профиль не существует или запрос не находит его.

**Решение:**
1. Проверьте, что профиль существует в таблице `profiles`
2. Проверьте, что `id` в профиле совпадает с `id` в `auth.users`
3. Выполните скрипт `scripts/create-missing-profiles.sql`

### Проблема: "clinic_id is NULL"

**Причина:** У пользователя не назначена клиника.

**Решение:**
1. Назначьте пользователю клинику (см. Решение 1)
2. Или убедитесь, что запрос работает без clinic_id (LEFT JOIN должен работать)

## Проверка после исправления

После применения исправлений:

1. **Очистите кэш браузера** (Ctrl+Shift+R)
2. **Попробуйте войти снова**
3. **Проверьте консоль браузера** на наличие ошибок
4. **Проверьте логи Supabase** на наличие ошибок

---

**См. также:**
- [USER_PROFILE_CREATION.md](USER_PROFILE_CREATION.md) - Создание профилей
- [TROUBLESHOOTING_LOADING.md](../TROUBLESHOOTING_LOADING.md) - Проблемы с загрузкой

