# Команды для пересборки приложения на сервере

## ⚠️ Важно: Vite и переменные окружения в production

**Проблема:** Vite встраивает переменные окружения с префиксом `VITE_` в код во время **сборки** (build time), а не во время выполнения (runtime). 

Это значит, что:
- Переменные должны быть доступны во время выполнения `npm run build`
- Просто наличие переменной в `.env` файле на сервере **не гарантирует**, что она будет встроена в сборку
- Если сборка была выполнена **до** добавления переменной в `.env`, то старая сборка не содержит эту переменную

**Решение:** 
1. Убедитесь, что переменная есть в `.env` или `.env.production` на сервере
2. **Пересоберите** приложение после добавления/изменения переменной
3. Для надежности используйте явный `export` перед сборкой (см. раздел 7)
4. **⚠️ После пересборки обязательно очистите кэш браузера** (см. раздел 10)

**Частая проблема:** После пересборки браузер может использовать старую версию из кэша (`304 Not Modified`). 
Выполните **Hard Refresh**: `Ctrl + Shift + R` (Windows/Linux) или `Cmd + Shift + R` (Mac).

---

## 1. Подключитесь к серверу по SSH

```bash
ssh ваш-пользователь@ваш-сервер
```

## 2. Перейдите в директорию проекта

```bash
cd /путь/к/вашему/проекту/psipilot-assistant
# Или если используете стандартные пути:
# cd ~/psipilot-assistant
# cd /var/www/psipilot-assistant
```

## 3. Проверьте наличие переменной в .env файле

**На production сервере используйте `.env` или `.env.production` (не `.env.local`!)**

```bash
# Проверьте какие .env файлы есть
ls -la .env*

# Проверьте содержимое переменной в .env (для production)
grep VITE_N8N_SUPERVISOR_WEBHOOK_URL .env

# ИЛИ если используете .env.production
grep VITE_N8N_SUPERVISOR_WEBHOOK_URL .env.production

# ИЛИ если используете .env.local (только для локальной разработки!)
grep VITE_N8N_SUPERVISOR_WEBHOOK_URL .env.local
```

**Важно:** Vite загружает переменные в следующем порядке приоритета для production:
1. `.env.production.local` (если есть)
2. `.env.local` (если есть) 
3. `.env.production` (если есть)
4. `.env` (базовый файл)

На production сервере лучше использовать `.env.production` или `.env`.

**Важно:** Убедитесь, что:
- Переменная начинается с `VITE_`
- URL указан полностью и правильно
- Нет лишних пробелов или кавычек

## 4. Проверьте формат переменной

```bash
# Должно быть так:
cat .env.local | grep VITE_N8N_SUPERVISOR_WEBHOOK_URL

# Пример правильного формата:
# VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://cigadusiref.beget.app/webhook/0c857cb1-9655-4af0-96ec-b1e1bc93b280
```

**Неправильно:**
```
VITE_N8N_SUPERVISOR_WEBHOOK_URL = https://...
VITE_N8N_SUPERVISOR_WEBHOOK_URL="https://..."
VITE_N8N_SUPERVISOR_WEBHOOK_URL 'https://...'
```

**Правильно:**
```
VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://cigadusiref.beget.app/webhook/0c857cb1-9655-4af0-96ec-b1e1bc93b280
```

## 5. Очистите кэш и старую сборку

```bash
# Удалите папку dist (старая сборка)
rm -rf dist

# Очистите node_modules/.vite (кэш Vite)
rm -rf node_modules/.vite

# Если используете кэш npm
npm cache clean --force
```

## 6. Убедитесь что зависимости установлены

```bash
npm install
```

## 7. Пересоберите приложение

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Все переменные окружения должны быть доступны во время сборки!

```bash
# Вариант 1: Обычная сборка (Vite автоматически загрузит .env, .env.production или .env.local)
npm run build

# Вариант 2: Явный экспорт всех необходимых переменных (РЕКОМЕНДУЕТСЯ для production)
export VITE_SUPABASE_URL=https://lulebraggalast.beget.app
export VITE_SUPABASE_ANON_KEY=ваш-anon-ключ-здесь
export VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://cigadusiref.beget.app/webhook/0c857cb1-9655-4af0-96ec-b1e1bc93b280

# Проверьте что переменные установлены
echo "Supabase URL: $VITE_SUPABASE_URL"
echo "Supabase Key: ${VITE_SUPABASE_ANON_KEY:0:20}..."
echo "Supervisor Webhook: $VITE_N8N_SUPERVISOR_WEBHOOK_URL"

# Теперь соберите
npm run build

# Вариант 3: В одну строку (для скриптов)
VITE_SUPABASE_URL=https://supershrimp.ru \
VITE_SUPABASE_ANON_KEY=ваш-ключ \
VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://cigadusiref.beget.app/webhook/... \
npm run build
```

**Рекомендация для production:** Всегда используйте вариант 2 (явный `export`) перед сборкой, чтобы гарантировать, что ВСЕ переменные точно будут доступны во время сборки.

## 8. Проверьте что переменные встроились в сборку

**ВАЖНО: После сборки откройте приложение в браузере и откройте консоль разработчика (F12).**
Вы должны увидеть подробную диагностику всех переменных окружения.

**Или проверьте напрямую в коде:**

```bash
# Проверьте наличие переменных в собранном коде
grep -r "VITE_SUPABASE_URL" dist/assets/*.js | head -1
grep -r "VITE_SUPABASE_ANON_KEY" dist/assets/*.js | head -1
grep -r "VITE_N8N_SUPERVISOR_WEBHOOK_URL" dist/assets/*.js | head -1

# Проверьте что реальные значения есть в коде (замените на ваши значения)
grep -r "supershrimp.ru" dist/assets/*.js && echo "✅ Supabase URL найден в сборке"
grep -r "cigadusiref.beget.app" dist/assets/*.js && echo "✅ Supervisor webhook URL найден в сборке"
```

**Если переменные не найдены:**
- Они НЕ были доступны во время сборки
- Пересоберите с явным `export` переменных (см. раздел 7)

## 9. Перезапустите веб-сервер (если нужно)

```bash
# Если используете pm2
pm2 restart ваш-процесс

# Или если используете systemd
sudo systemctl restart ваш-сервис

# Или просто перезапустите nginx/apache
sudo systemctl restart nginx
# ИЛИ
sudo systemctl restart apache2
```

## 10. ⚠️ КРИТИЧЕСКИ ВАЖНО: Очистите кэш браузера!

**После пересборки на сервере браузер может использовать старую версию из кэша!**

Если вы видите в Network tab браузера статус `304 Not Modified`, это означает, что браузер использует закэшированную версию файла.

**Решения:**

1. **Hard Refresh (принудительная перезагрузка):**
   - **Windows/Linux:** `Ctrl + Shift + R` или `Ctrl + F5`
   - **Mac:** `Cmd + Shift + R`
   - Это заставит браузер игнорировать кэш и загрузить свежие файлы

2. **Очистить кэш через DevTools:**
   - Откройте DevTools (F12)
   - Перейдите на вкладку **Network**
   - Включите опцию **"Disable cache"** (отключить кэш)
   - Перезагрузите страницу (`F5`)

3. **Очистить кэш браузера полностью:**
   - **Chrome/Edge:** `Ctrl + Shift + Delete` → выберите "Cached images and files"
   - **Firefox:** `Ctrl + Shift + Delete` → выберите "Cache"

4. **Использовать режим инкогнито/приватный:**
   - Откройте сайт в режиме инкогнито (кэш не используется)
   - Это поможет проверить, что новая сборка работает

**Проверка:**
- После hard refresh проверьте в Network tab, что файлы загружаются со статусом `200 OK` (не `304`)
- Откройте консоль (F12 → Console) и проверьте диагностику переменных окружения
- Убедитесь, что переменные показываются как установленные

## Альтернативный способ: Использование скрипта сборки

В корне проекта есть готовый скрипт `build-production.sh`, который:
- Проверяет наличие всех обязательных переменных
- Обнаруживает частые ошибки (пробелы после `=` в .env)
- Безопасно загружает переменные из .env
- Показывает частичные значения для проверки

**Использование:**

```bash
# Сделайте скрипт исполняемым (один раз)
chmod +x build-production.sh

# Запустите скрипт
./build-production.sh
```

**⚠️ ВАЖНО:** Перед запуском исправьте `.env` файл:

**Неправильно (с пробелом):**
```
VITE_TRANSCRIPTION_API_URL= https://supershrimp.ru
```

**Правильно (без пробела):**
```
VITE_TRANSCRIPTION_API_URL=https://supershrimp.ru
```

Скрипт автоматически проверит формат переменных и предупредит, если обнаружит проблемы.

## Диагностика проблем

Если после пересборки всё ещё не работает:

1. **Проверьте что переменная доступна во время сборки:**
```bash
# Сначала экспортируйте переменную (если используете этот способ)
export VITE_N8N_SUPERVISOR_WEBHOOK_URL=https://ваш-url

# Проверьте что она доступна
node -e "console.log('VITE_N8N_SUPERVISOR_WEBHOOK_URL:', process.env.VITE_N8N_SUPERVISOR_WEBHOOK_URL)"

# Или проверьте через echo
echo $VITE_N8N_SUPERVISOR_WEBHOOK_URL
```

2. **Проверьте логи сборки на наличие ошибок:**
```bash
npm run build 2>&1 | tee build.log
```

3. **Проверьте собранный код напрямую:**
```bash
# Найдите файл где используется переменная
grep -r "N8N_SUPERVISOR" dist/assets/*.js | head -5
```

4. **Откройте приложение в браузере и проверьте консоль:**
   - Откройте Developer Tools (F12)
   - Перейдите на вкладку Console
   - При загрузке приложения должна появиться подробная диагностика переменных окружения
   - Найдите раздел "🔍 Диагностика переменных окружения"
   - Проверьте, какие переменные установлены, а какие нет

5. **Если переменные отсутствуют в диагностике:**
   - Это означает, что они не были встроены во время сборки
   - Убедитесь, что использовали `export` перед `npm run build`
   - Проверьте, что переменные доступны: `echo $VITE_SUPABASE_URL`

6. **⚠️ Проверьте кэш браузера:**
   - Если видите `304 Not Modified` в Network tab → браузер использует старую версию
   - Выполните **Hard Refresh**: `Ctrl + Shift + R` (Windows/Linux) или `Cmd + Shift + R` (Mac)
   - Или используйте режим инкогнито для проверки
   - После hard refresh файлы должны загружаться со статусом `200 OK`
