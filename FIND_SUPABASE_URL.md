# Как найти URL для self-hosted Supabase

Для self-hosted Supabase URL может быть в разных местах в зависимости от способа установки.

## 🔍 Где искать URL

### 1. Docker Compose (если используете Docker)

Найдите файл `docker-compose.yml` или `.env` в папке с Supabase:

```bash
# Обычно в файле docker-compose.yml есть:
services:
  kong:
    ports:
      - "8000:8000"  # Это API Gateway порт
```

**URL будет:** `http://localhost:8000` (или ваш домен)

### 2. Supabase CLI (если используете `supabase start`)

```bash
# Запустите команду:
supabase status

# Вы увидите что-то вроде:
# API URL: http://localhost:54321
# или
# API URL: http://127.0.0.1:8000
```

### 3. Конфигурационные файлы

Проверьте файлы в папке вашего Supabase проекта:

- `supabase/.env` или `.env`
- `supabase/config.toml`
- `docker-compose.yml`

Ищите переменные:
- `API_URL`
- `SUPABASE_URL`
- `KONG_HTTP_PORT` (обычно 8000)
- `KONG_HTTPS_PORT`

### 4. Проверка через браузер

Попробуйте открыть в браузере:

- `http://localhost:8000` - стандартный порт API Gateway
- `http://localhost:54321` - если используете Supabase CLI локально
- `http://localhost:3000` - если настроен кастомный порт

Если открывается страница с JSON или API документацией - это правильный URL!

### 5. Через Docker (если используете)

```bash
# Посмотрите запущенные контейнеры
docker ps

# Найдите контейнер с названием типа "supabase-kong" или "supabase_api"
# Посмотрите порты (обычно 8000:8000)
```

### 6. Логи Supabase

Проверьте логи запуска Supabase - там обычно выводится URL:

```bash
# Если используете Docker
docker-compose logs | grep -i "url\|api\|kong"

# Если используете Supabase CLI
supabase status
```

## 📝 Стандартные порты для self-hosted Supabase

- **8000** - API Gateway (Kong) - это то, что нужно!
- **54321** - Studio (веб-интерфейс)
- **54322** - PostgreSQL
- **54323** - PostgREST

## ✅ Правильный формат URL

URL должен быть в формате:
```
http://localhost:8000
```
или
```
https://your-domain.com
```

**НЕ должен быть:**
- `http://localhost:54321` (это Studio, не API)
- `http://localhost:3000` (это ваш фронтенд)

## 🔧 Как проверить правильность URL

1. Откройте URL в браузере
2. Должна открыться страница с JSON или API endpoint
3. Или попробуйте: `http://ваш-url/rest/v1/` - должно вернуть информацию об API

## 💡 Если не можете найти

Попробуйте эти команды в терминале:

**Windows PowerShell:**
```powershell
# Проверьте, какие порты слушают
netstat -ano | findstr :8000
netstat -ano | findstr :54321

# Или проверьте Docker контейнеры
docker ps
```

**Linux/Mac:**
```bash
# Проверьте порты
lsof -i :8000
lsof -i :54321

# Или Docker
docker ps
```











