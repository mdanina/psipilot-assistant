# Настройка безопасности для HIPAA/GDPR/152-ФЗ

Этот документ описывает настройку критических функций безопасности, реализованных в проекте.

## 🔐 Настройка шифрования (Field-Level Encryption)

### Генерация ключа шифрования

Для шифрования PHI данных на уровне приложения требуется ключ шифрования.

**Генерация ключа:**

```bash
# Linux/Mac
openssl rand -base64 32

# Windows (PowerShell)
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

**Добавьте ключ в `.env.local`:**

```env
VITE_ENCRYPTION_KEY=ваш-сгенерированный-ключ-здесь
```

**ВАЖНО:**
- Никогда не коммитьте ключ в git
- Храните ключ в секретном менеджере для production
- Используйте разные ключи для dev/staging/production
- Регулярно ротируйте ключи (раз в год или при компрометации)

### Применение миграций

Выполните новую миграцию для поддержки шифрования и MFA:

```sql
-- В Supabase SQL Editor выполните:
-- supabase/migrations/005_mfa_and_security.sql
```

## 🔑 Настройка MFA (Multi-Factor Authentication)

### Включение MFA для пользователя

MFA настраивается через UI компоненты (будут созданы отдельно) или через Supabase Auth API.

**Через Supabase Dashboard:**
1. Settings → Authentication → MFA
2. Включите TOTP MFA
3. Пользователи смогут настроить MFA в своем профиле

**Через код:**
```typescript
import { useAuth } from '@/contexts/AuthContext';

const { enableMFA, verifyMFA } = useAuth();

// Начать настройку MFA
const { data, error } = await enableMFA();
// data содержит qrCode и secret для настройки

// Верифицировать код
await verifyMFA('123456'); // 6-значный код из приложения
```

## ⏱️ Session Timeout

Session timeout настроен автоматически на 15 минут неактивности.

**Настройка (опционально):**

В `src/contexts/AuthContext.tsx` измените константу:
```typescript
const SESSION_TIMEOUT = 15 * 60 * 1000; // 15 минут
```

Или добавьте в `.env.local`:
```env
VITE_SESSION_TIMEOUT=900000  # 15 минут в миллисекундах
```

## 📋 READ Audit Logging

READ операции автоматически логируются при использовании `auditedSupabase` клиента.

**Использование:**

```typescript
import { auditedSupabase } from '@/lib/supabase-audited';

// Вместо supabase.from('patients').select()
const { data } = await auditedSupabase
  .from('patients')
  .select('*');
// Автоматически логируется в audit_logs
```

**Просмотр логов:**

```sql
SELECT * FROM audit_logs 
WHERE action = 'read' 
ORDER BY created_at DESC 
LIMIT 100;
```

## ✅ Проверка активных согласий

RLS политики автоматически проверяют активные согласия перед доступом к данным.

**Требования:**
- Пациент должен иметь активное согласие `data_processing` для доступа к данным
- Для записей требуется согласие `recording`

**Создание согласия:**

```sql
INSERT INTO consent_records (
    patient_id,
    consent_type,
    consent_purpose,
    legal_basis,
    status,
    consent_method,
    collected_by
) VALUES (
    'patient-uuid',
    'data_processing',
    'Обработка медицинских данных для ведения клинических записей',
    'consent',
    'active',
    'electronic',
    'user-uuid'
);
```

## 🔒 Использование зашифрованного клиента

Для автоматического шифрования/дешифрования PHI данных:

```typescript
import { encryptedSupabase } from '@/lib/supabase-encrypted';

// При записи - автоматически шифруется
await encryptedSupabase
  .from('clinical_notes')
  .insert({
    ai_summary: 'Чувствительные данные', // Автоматически зашифруется
  });

// При чтении - автоматически расшифровывается
const { data } = await encryptedSupabase
  .from('clinical_notes')
  .select('*');
// data.ai_summary автоматически расшифрован
```

## 🛡️ Комбинированное использование (максимальная безопасность)

Для одновременного шифрования и аудита:

```typescript
import { secureSupabase } from '@/lib/supabase-audited';

// Автоматически: шифрование + аудит
const { data } = await secureSupabase
  .from('clinical_notes')
  .select('*');
```

## 📊 Проверка соответствия

### Чеклист безопасности:

- [ ] Ключ шифрования сгенерирован и добавлен в `.env.local`
- [ ] Миграция `005_mfa_and_security.sql` применена
- [ ] MFA включена для администраторов
- [ ] Session timeout работает (проверьте через 15 минут неактивности)
- [ ] READ операции логируются (проверьте `audit_logs`)
- [ ] Согласия созданы для тестовых пациентов
- [ ] RLS политики проверяют согласия (попробуйте доступ без согласия)

### Тестирование:

1. **Шифрование:**
   ```typescript
   import { encryptPHI, decryptPHI } from '@/lib/encryption';
   const encrypted = await encryptPHI('test data');
   const decrypted = await decryptPHI(encrypted);
   console.log(decrypted === 'test data'); // true
   ```

2. **Аудит:**
   - Выполните SELECT запрос через `auditedSupabase`
   - Проверьте `audit_logs` таблицу

3. **Согласия:**
   - Создайте пациента без согласия
   - Попробуйте получить доступ - должно быть запрещено
   - Создайте согласие - доступ должен появиться

## 🚨 Важные замечания

1. **Миграция данных:** При первом включении шифрования существующие данные нужно зашифровать
2. **Резервное копирование:** Всегда делайте бэкап перед применением миграций
3. **Тестирование:** Протестируйте все функции на dev окружении перед production
4. **Мониторинг:** Регулярно проверяйте `audit_logs` на подозрительную активность

## 📚 Дополнительные ресурсы

- [HIPAA Security Rule](https://www.hhs.gov/hipaa/for-professionals/security/index.html)
- [GDPR Article 32](https://gdpr-info.eu/art-32-gdpr/)
- [Supabase MFA Documentation](https://supabase.com/docs/guides/auth/mfa)

