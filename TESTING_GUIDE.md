# 🧪 Руководство по тестированию изменений производительности

## 📋 Чеклист тестирования

### ✅ Шаг 1: Базовое тестирование (проверка, что ничего не сломалось)

#### 1.1. Запустить приложение

```bash
# Убедиться, что зависимости установлены
npm install

# Запустить dev сервер
npm run dev
```

**Ожидаемый результат:** Приложение запускается без ошибок

#### 1.2. Проверить основные страницы

**Тест 1: Страница входа**
- [ ] Открыть `/login`
- [ ] Войти с существующими учетными данными
- [ ] Проверить, что авторизация работает

**Тест 2: Страница пациентов**
- [ ] Открыть `/patients`
- [ ] Проверить, что список пациентов загружается
- [ ] Проверить, что поиск работает
- [ ] Проверить, что можно создать нового пациента

**Тест 3: Страница сессий**
- [ ] Открыть `/sessions`
- [ ] Проверить, что сессии загружаются
- [ ] Проверить, что можно создать новую сессию

**Тест 4: Другие страницы**
- [ ] Проверить все остальные страницы приложения
- [ ] Убедиться, что навигация работает

**Ожидаемый результат:** Все страницы работают как раньше, без ошибок

---

### ✅ Шаг 2: Проверка кеширования React Query

#### 2.1. Открыть DevTools браузера

**Chrome/Edge:**
- Нажать `F12` или `Ctrl+Shift+I` (Windows) / `Cmd+Option+I` (Mac)
- Перейти на вкладку **Network** (Сеть)

**Firefox:**
- Нажать `F12`
- Перейти на вкладку **Network**

#### 2.2. Тест кеширования на странице пациентов

**Шаги:**
1. Открыть `/patients`
2. В Network tab очистить запросы (кнопка 🚫)
3. **Первый раз:** Загрузить страницу
   - В Network tab должен быть запрос к Supabase (обычно к `rest/v1/patients`)
   - Записать время загрузки
4. **Перейти на другую страницу** (например, `/sessions`)
5. **Вернуться на `/patients`**
   - В Network tab НЕ должно быть нового запроса к `patients` (если прошло меньше 5 минут)
   - Это означает, что данные взяты из кеша!

**Ожидаемый результат:** 
- ✅ Первая загрузка: запрос к БД есть
- ✅ Повторная загрузка (в течение 5 минут): запроса нет (данные из кеша)

#### 2.3. Проверить кеш в React Query DevTools (опционально)

**Установить React Query DevTools:**

```bash
npm install @tanstack/react-query-devtools --save-dev
```

**Добавить в `src/App.tsx`:**

```typescript
import { ReactQueryDevtools } from '@tanstack/react-query-devtools';

const App = () => (
  <ThemeProvider>
    <QueryClientProvider client={queryClient}>
      {/* ... существующий код ... */}
      
      {/* Добавить DevTools (только в dev режиме) */}
      {import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
    </QueryClientProvider>
  </ThemeProvider>
);
```

**Использование:**
- После установки в браузере появится иконка React Query
- Можно видеть все кешированные запросы
- Можно видеть, когда данные считаются "stale" (устаревшими)

---

### ✅ Шаг 3: Измерение производительности

#### 3.1. Измерить время загрузки страниц

**Способ 1: Chrome DevTools Performance**

1. Открыть Chrome DevTools (`F12`)
2. Перейти на вкладку **Performance**
3. Нажать кнопку **Record** (⏺️)
4. Перезагрузить страницу (`Ctrl+R`)
5. Подождать полной загрузки
6. Нажать **Stop** (⏹️)
7. Посмотреть метрики:
   - **Load:** Время полной загрузки
   - **DOMContentLoaded:** Время загрузки DOM

**Способ 2: Lighthouse**

1. Открыть Chrome DevTools (`F12`)
2. Перейти на вкладку **Lighthouse**
3. Выбрать категории: **Performance**
4. Нажать **Analyze page load**
5. Посмотреть метрики:
   - **First Contentful Paint (FCP)**
   - **Time to Interactive (TTI)**
   - **Largest Contentful Paint (LCP)**

**Способ 3: Network tab**

1. Открыть Network tab (`F12` → Network)
2. Очистить запросы
3. Перезагрузить страницу (`Ctrl+R`)
4. Посмотреть:
   - **Время загрузки страницы** (внизу: "Load: X ms")
   - **Количество запросов** к Supabase
   - **Размер данных**

#### 3.2. Сравнить "до" и "после"

**До изменений (если есть backup):**
```bash
# Откатить изменения временно
git stash

# Запустить приложение
npm run dev

# Измерить производительность (записать результаты)
# - Время загрузки /patients: _____ ms
# - Количество запросов: _____
# - Размер данных: _____ KB
```

**После изменений:**
```bash
# Вернуть изменения
git stash pop

# Запустить приложение
npm run dev

# Измерить производительность (записать результаты)
# - Время загрузки /patients: _____ ms
# - Количество запросов: _____
# - Размер данных: _____ KB
```

**Ожидаемые улучшения:**
- ✅ Время загрузки при повторном открытии: **-50-70%**
- ✅ Количество запросов при повторном открытии: **-80-100%** (кеш работает)
- ✅ Размер данных: без изменений (или меньше из-за кеширования)

---

### ✅ Шаг 4: Тестирование в разных сценариях

#### 4.1. Тест переключения между страницами

**Шаги:**
1. Открыть `/patients` (первая загрузка - запрос к БД)
2. Перейти на `/sessions` (новая страница)
3. Вернуться на `/patients` (должен использоваться кеш - запроса нет)
4. Подождать 6 минут
5. Вернуться на `/patients` (кеш устарел - должен быть новый запрос)

**Ожидаемый результат:**
- ✅ Быстрое переключение между страницами (данные из кеша)
- ✅ Автоматическое обновление после истечения кеша (5 минут)

#### 4.2. Тест с медленным интернетом

**Chrome DevTools:**
1. Открыть DevTools (`F12`)
2. Перейти на вкладку **Network**
3. В выпадающем списке выбрать **Slow 3G** или **Fast 3G**
4. Перезагрузить страницу
5. Проверить, что кеширование помогает при медленном интернете

**Ожидаемый результат:**
- ✅ При повторном открытии страницы данные загружаются мгновенно (из кеша)
- ✅ Не нужно ждать запроса к серверу

#### 4.3. Тест с отключенным интернетом

**Шаги:**
1. Открыть `/patients` (данные загружены и закешированы)
2. В Chrome DevTools → Network → выбрать **Offline**
3. Перейти на другую страницу
4. Вернуться на `/patients`

**Ожидаемый результат:**
- ✅ Данные показываются из кеша (если кеш не устарел)
- ✅ Пользователь видит данные, даже без интернета

---

### ✅ Шаг 5: Проверка консоли на ошибки

#### 5.1. Открыть Console

1. Открыть DevTools (`F12`)
2. Перейти на вкладку **Console**
3. Проверить наличие ошибок (красные сообщения)

**Ожидаемый результат:**
- ✅ Нет ошибок, связанных с React Query
- ✅ Нет ошибок, связанных с загрузкой данных
- ✅ Могут быть предупреждения (warnings), но не критичные ошибки

#### 5.2. Проверить логи в терминале

**Backend (если запущен):**
```bash
# В терминале, где запущен backend
# Проверить, нет ли ошибок
```

**Frontend:**
```bash
# В терминале, где запущен npm run dev
# Проверить, нет ли ошибок компиляции
```

**Ожидаемый результат:**
- ✅ Нет ошибок компиляции
- ✅ Нет ошибок выполнения

---

### ✅ Шаг 6: Тестирование edge cases

#### 6.1. Тест с пустыми данными

**Шаги:**
1. Создать нового пользователя (без пациентов)
2. Открыть `/patients`
3. Проверить, что страница работает корректно

**Ожидаемый результат:**
- ✅ Показывается пустой список (не ошибка)
- ✅ Можно создать нового пациента

#### 6.2. Тест с большим количеством данных

**Шаги:**
1. Создать много пациентов (если возможно)
2. Открыть `/patients`
3. Проверить производительность

**Ожидаемый результат:**
- ✅ Страница загружается в разумное время
- ✅ Кеширование работает для большого объема данных

#### 6.3. Тест с ошибками сети

**Шаги:**
1. Открыть `/patients`
2. В DevTools → Network → выбрать **Offline**
3. Попытаться обновить данные (если есть кнопка "Обновить")

**Ожидаемый результат:**
- ✅ Показывается сообщение об ошибке (если кеш устарел)
- ✅ Или показываются данные из кеша (если кеш свежий)

---

## 🐛 Что делать, если что-то сломалось

### Проблема 1: Приложение не запускается

**Решение:**
```bash
# Проверить ошибки компиляции
npm run dev

# Если есть ошибки TypeScript
npm run build

# Откатить изменения
git checkout HEAD -- src/App.tsx src/lib/query-client.ts
```

### Проблема 2: Страницы не загружаются

**Решение:**
1. Открыть Console в браузере (`F12` → Console)
2. Посмотреть ошибки
3. Если ошибка связана с React Query - проверить импорты
4. Если не помогает - откатить изменения

### Проблема 3: Данные не обновляются

**Решение:**
- Это нормально! Кеш работает 5 минут
- Чтобы принудительно обновить: перезагрузить страницу (`Ctrl+R`)
- Или подождать 5+ минут

### Проблема 4: Нужно откатить все изменения

**Решение:**
```bash
# Откатить изменения в Git
git checkout HEAD -- src/App.tsx src/lib/query-client.ts

# Удалить созданные файлы (если нужно)
rm src/lib/query-client.ts
rm src/hooks/usePatients.example.ts
```

---

## 📊 Шаблон для записи результатов

```
Дата тестирования: ___________
Тестировщик: ___________

### Базовое тестирование
[ ] Приложение запускается
[ ] Страница входа работает
[ ] Страница пациентов работает
[ ] Страница сессий работает
[ ] Другие страницы работают

### Тестирование кеширования
Первая загрузка /patients:
- Время: _____ ms
- Запросов к БД: _____
- Размер данных: _____ KB

Повторная загрузка /patients (в течение 5 минут):
- Время: _____ ms
- Запросов к БД: _____ (должно быть 0)
- Размер данных: _____ KB

### Производительность
First Contentful Paint (FCP): _____ ms
Time to Interactive (TTI): _____ ms
Largest Contentful Paint (LCP): _____ ms

### Ошибки
[ ] Нет ошибок в Console
[ ] Нет ошибок в терминале
[ ] Нет ошибок в Network tab

### Замечания
_______________________________________
_______________________________________
_______________________________________
```

---

## ✅ Критерии успешного тестирования

Тестирование считается успешным, если:

1. ✅ Приложение запускается без ошибок
2. ✅ Все страницы работают как раньше
3. ✅ Кеширование работает (повторные загрузки быстрее)
4. ✅ Нет новых ошибок в консоли
5. ✅ Производительность улучшилась (или не ухудшилась)

---

## 🎯 Следующие шаги после успешного тестирования

Если все тесты пройдены:

1. ✅ Закоммитить изменения:
   ```bash
   git add src/lib/query-client.ts src/App.tsx
   git commit -m "feat: add React Query caching configuration"
   ```

2. ✅ Продолжить миграцию компонентов (опционально):
   - Следовать плану из `PERFORMANCE_FIX_PLAN.md`
   - Мигрировать по одному компоненту
   - Тестировать после каждого изменения

3. ✅ Мониторить производительность в production:
   - Использовать аналитику (Google Analytics, etc.)
   - Отслеживать метрики производительности

---

**Готово!** Теперь вы знаете, как протестировать изменения и убедиться, что все работает правильно.

