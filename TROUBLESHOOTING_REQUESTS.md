# 🔍 Диагностика увеличения количества запросов

## ⚠️ Важно понять разницу

В Network tab видны **два типа запросов**:

1. **Запросы к скриптам (JS файлы)** - это нормально при первой загрузке
2. **Запросы к данным (API к Supabase)** - вот это нужно проверять

## 🎯 Как проверить только запросы к данным

### Шаг 1: Применить фильтр в Network tab

1. Открыть DevTools (`F12`) → вкладка **Network**
2. Нажать на кнопку **"Fetch/XHR"** (вверху, рядом с "All")
3. Это покажет **только запросы к API**, скрывая скрипты

### Шаг 2: Проверить запросы к пациентам

После применения фильтра вы должны видеть только:
- `patients?select=*` - запрос к списку пациентов
- `documents?select=patient_id&...` - запрос к документам
- `decrypt` - запросы на расшифровку

**Ожидаемое поведение:**
- **Первая загрузка:** 2-3 запроса (patients + documents + возможно decrypt)
- **Повторная загрузка (в течение 5 минут):** 0 запросов (кеш работает)

---

## 🔴 Возможные причины увеличения запросов

### Причина 1: Первая загрузка страницы (нормально)

Если вы видите много запросов к `.js` файлам - это **нормально** при первой загрузке. Это скрипты приложения, они кешируются браузером.

**Решение:** Применить фильтр "Fetch/XHR" чтобы видеть только API запросы.

### Причина 2: React Query делает retry запросы

Если запрос падает с ошибкой, React Query автоматически повторяет его (retry: 3).

**Решение:** Я уже уменьшил retry до 1 в hooks. Проверьте Console на ошибки.

### Причина 3: Поиск создает запросы при каждом изменении

Если вы вводите текст в поиск, каждый символ может создавать новый запрос.

**Решение:** Я добавил debounce - теперь поиск работает только если введено минимум 2 символа.

### Причина 4: Множественные компоненты используют один hook

Если `usePatients()` вызывается в нескольких местах одновременно.

**Решение:** React Query автоматически дедуплицирует запросы с одинаковым `queryKey`.

---

## ✅ Что я исправил

1. ✅ Уменьшил retry с 3 до 1 (меньше повторных запросов при ошибках)
2. ✅ Добавил debounce для поиска (минимум 2 символа)
3. ✅ Отключил `refetchOnWindowFocus` в hooks (не рефетчить при фокусе окна)

---

## 🧪 Как правильно проверить

### Правильный тест:

1. Открыть DevTools (`F12`) → вкладка **Network**
2. Нажать **"Fetch/XHR"** (фильтр для API запросов)
3. Очистить запросы (кнопка 🚫)
4. Открыть `/patients` → должны быть 2-3 запроса к API
5. Перейти на другую страницу
6. Вернуться на `/patients` → **запросов быть не должно!**

**Если запросов нет при повторном открытии - кеширование работает! ✅**

---

## 📊 Что считать нормальным

### Нормально:
- ✅ 200+ запросов к скриптам при первой загрузке (это JS файлы)
- ✅ 2-3 запроса к API при первой загрузке данных
- ✅ 0 запросов к API при повторной загрузке (кеш работает)

### Проблема:
- ❌ Много запросов к `patients?select=*` при каждом открытии страницы
- ❌ Запросы к API при каждом изменении поиска (без debounce)

---

## 🔧 Если проблема сохраняется

1. **Проверьте Console на ошибки:**
   - Откройте Console (`F12` → Console)
   - Посмотрите, есть ли красные ошибки
   - Ошибки могут вызывать retry запросы

2. **Проверьте фильтр в Network:**
   - Убедитесь, что вы смотрите на "Fetch/XHR", а не "All"
   - "All" показывает все запросы, включая скрипты

3. **Проверьте, не открываете ли вы страницу в первый раз:**
   - Первая загрузка всегда делает запросы
   - Проверьте повторную загрузку (второй раз открыть страницу)

---

## 💡 Важно

**Много запросов к скриптам (`.js` файлы) - это НОРМАЛЬНО!**

Это файлы приложения, которые загружаются один раз и кешируются браузером. При повторной загрузке страницы они будут показывать статус `304 Not Modified` (из кеша).

**Главное - проверить запросы к данным (API):**
- Примените фильтр "Fetch/XHR"
- Проверьте запросы к `patients?select=*`
- При повторном открытии их быть не должно

---

**Попробуйте применить фильтр "Fetch/XHR" и проверить только API запросы!**

